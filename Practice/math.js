const plus = (a, b) => a + b;
const minus = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
export { plus, minus, multiply, divide };

// var Modules = {
//   plus,
//   minus,
//   multiply,
//   divide,
// };
// export default Modules;
